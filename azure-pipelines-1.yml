trigger: none

pool:
  vmImage: ubuntu-latest

steps:
- task: JfrogCliV2@1
  displayName: 'Run SAST Scan'
  inputs:
    jfrogPlatformConnection: 'JFrogPlatformV2Connection' #project settingsで作成した名前
    command: 'jf --version'
    # command: 'jf aud --sast --secrets'